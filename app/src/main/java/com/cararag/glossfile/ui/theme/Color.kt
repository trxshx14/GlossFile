package com.cararag.glossfile.ui.theme

import androidx.compose.ui.graphics.Color

// Base neutrals — soft cream canvas
val Cream = Color(0xFFFDF8F3)
val CreamCard = Color(0xFFFFFFFF)
val Ink = Color(0xFF4A3F3A)          // warm dark-brown for primary text
val InkSoft = Color(0xFF9A8C84)      // muted secondary text

// Pastel accents
val Peach = Color(0xFFF7C9B6)
val BlushPink = Color(0xFFF3B9C4)
val DustyRose = Color(0xFFE0A6A6)
val Sage = Color(0xFFB8D0BD)

// Freshness states — intentionally NOT an alarm red.
// A product close to expiring fades to soft amber, then dusty rose.
val FreshGreen = Color(0xFFAFCBB4)   // calm sage = plenty of time
val SoonAmber = Color(0xFFEBC79A)    // soft amber = use it up
val ExpiredRose = Color(0xFFD79898)  // dusty rose = past PAO

val TrackGrey = Color(0xFFF0E7E0)    // empty bar track